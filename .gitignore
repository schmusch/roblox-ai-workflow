# Upstream RCS clone (separate repo, not part of this project)
roblox-ai-os/

# Roblox Studio temporary & lock files
*.lock
*.log

# OS generated files
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE local state
.vscode/
.idea/
*.swp
*.swo
*~

# Node (if anyone adds tooling later)
node_modules/

# Build artifacts (if anyone adds tooling later)
dist/
build/
*.tmp

# Local Claude Code session state (if accidentally placed here)
.claude/

# Local secrets — never commit
.env
.env.*
!.env.example

# Wally packages (if used)
.wally/
Packages/
ServerPackages/

# Rojo build artifacts
*.rbxlx.temp
